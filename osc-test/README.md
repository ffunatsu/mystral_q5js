# Mystral OSC test

Minimal test for sending OSC messages with `node-osc` in Node.js and Mystral Native.

## Run

Run these commands in this directory.

```powershell
npm install
npm run test:node
npm run test:mystral
```

`test:node` verifies sending in Node.js. `test:mystral` checks Mystral Native compatibility.
